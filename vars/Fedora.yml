mariadb_packages:
  - '{{ "MariaDB-server"
        if (install_from == "mariadb_repository")
        else "mariadb-server" }}'
  - MySQL-python

mariadb_service: '{{ "mariadb"
                     if (ansible_distribution_major_version >= "27")
                     else "mysql" }}'
